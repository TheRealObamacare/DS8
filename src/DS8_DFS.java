public class DS8_DFS
{
    public static boolean depthFirstSearch_Simple(char[][]maze)
    {
        boolean visited = new boolean[maze.length][maze[0].length];
        DS8_Stack stack = new DS8_Stack();

        stack.push();
    }
}